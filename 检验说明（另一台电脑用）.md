# 群相 · 检验包使用说明（给另一台电脑用）

> 目的：在**另一台电脑**上跑一遍，把结果文件发回来，我据此定位与改进。
> 这是**在线包**（不含 Python 运行时与依赖）：**第一次运行需要能上网**——脚本会自己下载绿色版 Python 并装依赖（约 1~3 分钟），之后就不用再下了。

## 一、拷过去之后要做什么（三步）

1. 把 `群相-在线包-*.zip` 拷到那台电脑，**解压到桌面**（路径不要带空格更稳）。
2. 双击 **`一键检验（生成报告）.cmd`**
   - 它会等约 10 秒，期间会做一次"投递发送"实测：往【文件传输助手】发一条测试消息（**只有你自己能看到**，随手删掉即可）；
   - 跑完自动打开 `报告` 文件夹。
3. 把 `报告` 里最新的 **`检验报告-*.txt`**（连同同名 `.json` 更佳）发回来。

如果只是想先看看环境、**不想发任何消息**，双击 **`一键体检（只读，不发消息）.cmd`** 即可。

## 二、报告里有什么（我拿到就能定位）

| 小节 | 内容 | 能定位什么 |
|---|---|---|
| 一、系统与显示 | 操作系统/位数 · **DPI 上下文** · 物理分辨率 vs 进程所见（缩放几倍）· 显示器数量 | DPI 缩放导致的点击错位、多屏问题 |
| 二、微信与窗口 | 微信版本与安装路径 · **主窗类名/矩形/是否带渲染子窗** · 输入后端档位 | 是不是旧版 UI（3.x 类名不同）、窗口几何异常 |
| 三、依赖与能力 | 24 项依赖逐条 · **版本能力矩阵**（哪个能力 ok/no/未实测）· UIA 节点数 | 缺依赖、微信版本没实测过、UIA 树有没有物化 |
| 四、会话头可读性 | 会话头指纹能不能抓到 · **探测到的聊天面板左沿 px** | 指纹闸失效、面板宽度与标定不符 |
| 五、投递发送实测 | 真发一条测试消息的结果（成功/失败 + 原因 + token） | 投递链路在那台机器上到底通不通 |

## 三、那台机器上可能遇到的已知情况（先看这里）

- **微信窗口被最小化/收进托盘时**：投递发送大概率**不生效**（已记在案的已知限制）；先把微信窗口正常显示再跑。指纹抓取在被遮挡时也会失败。
- **微信以管理员运行、而脚本不是**：系统级限制（UIPI）会挡掉消息投递 ⇒ 报告第五节会失败，把它一起发回来即可确认。
- **系统缩放不是 150%**（100%/125%/175%/200%）：本包会如实记录缩放值；若投递失败，报告能直接看出是不是坐标口径的问题。
- **微信版本比 4.1.15.8 新**：报告第三节会显示"没实测记录"——这是**刻意设计**的（没跑过的版本不许写"支持"），把报告发回来我加一条实测记录。
- **想看界面**：双击 `一键启动.exe`（自带 WebView2 窗口，可拖动标题带、可拉四边缩放；那台机器若没装 WebView2 运行时才会退回浏览器，退回原因写在 `logs\console_open.log`）。控制台里的 **「点击测试」** 是 55 项完整检验，全程序内完成、**不碰鼠标、不抢前台**。

## 四、红线（这个包不会做什么）

- 不往微信进程写内存、不改微信文件、不注入 DLL、不装驱动；
- 不点「发表」、不点赞、不评论（这些是**对外可见**的真实动作，需要你点头）；
- 不采集设备指纹、不上传任何东西；所有报告都只落在本机的 `报告` 文件夹里。
