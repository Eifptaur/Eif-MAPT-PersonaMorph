# 开发任务清单（铁律：新任务必须登记；验证通过才核销；做完核查下一项）

> 规则（见 skill：wx-dev / wx-verify）：用户每次提出新任务 → 立即登记到本文件（状态=进行中）；
> 完成 = 通过 wx-verify 清单（编译/运行/联动/包验证四层证据）→ 才能标记 ✅ 并核销；
> 每完成一项工作，核查本清单，继续下一项；未验证的项不得标 ✅。

## 当前任务

- [✅] **2026-09-14 晚 · 用户点名的四项核查**：
  ① **"公告 + 检测更新 + 只下变更不用全包"能不能走通** ⇒ `rehearse-patch` **15/0**（增量补丁本体：只写变更文件 + 逐文件 sha256 + 中途失败回滚 + 路径穿越拒绝）；
  `rehearse-github` 例①（GitHub Releases 302 形态）**全绿**——经 302 取清单 → 认出新版本 → 判定可增量（`patch.from == 本地版本`）→ **只下 2 个变更文件** → 文件真被换掉 → 更新器日志确认走的是增量；
  例③（真实发布产物 385→387）**走了增量（3 文件）**但**收尾两条红**（更新器日志没确认完成、安装目录版本文件仍是 385）。
  ⛔ **仍红、需改 C# 客户端**：例② 镜像形态（直连被墙时经镜像前缀取清单失败 + 载荷地址不继承"取清单时生效的那个源"）。
  ⚠️ 顺带修掉**演练台自己**的 bug：`rehearse-github.js` 把**中文发布名**塞进 `Location` 头 → Node 抛 `ERR_INVALID_CHAR` → 台子崩 → 例①②整片被照成红（修完例①才显绿）。
  ② **功能映射**：`scripts/fullcheck.py` **112 项 0 失败**（原 6 红：4 条判据没跟上——vbs 已收进 `scripts\`、价目换成 2026-09-10 闲时价、cursor 64×64 采样口径写错；1 条是真的——两张角色卡重名「张飞」）。
  ③ **UI 分门别类**：新判据 `scripts/ui_arch_selftest.py` **10/0**，数字＝**24 块面板 · 24 条导航 · 206 个可调项**；每块都有标题与说明、导航无死链、**无孤儿配置**。今天补：漏在导航外的「群友要图」面板加进导航（自绘「要图」图标）+ 6 块缺说明的面板补 `desc`（人设与响应 / 记忆共享设置 / 服务器 / 界面适配 / 运行日志 / 完整配置 JSON）。
  ④ **禁言与响应档位**：`tier_control_selftest` **59/0**（固定 4 档 / 峰谷映射含跨午夜 / 指令禁言 @机器人「禁言 15」/ 白名单 / 到期恢复 / **真函数集成** `resolve_context_tier`）· `risk_selftest` **44/0** · `cfg_keys_selftest` 20/20 · UI 四件 50/50 · 18/0 · 31/31 · 57/57。
- [⏳] **测机报告队列（2026-09-14 那台机器实测推动）**：③ 坑 8/9/10 已修（启动器步骤 635 + 补丁守护随包）；
  **④ OCR 硬超时已做**（`agent/chat_ocr.py`：单次 25 秒硬上限 + 连续 2 次熔断 120 秒 + 发送链 60 秒总窗；
  判据 `scripts/ocr_timeout_selftest.py` 45/0；体检里新增「识别·OCR 卡顿」）；
  **⑥ 启动器「全局技能库」假红已修**（`dsh启动器` 提交 a960a9a：两个去处都数、取多的那个并把来源写进文案，
  缺全局目录不再是错误；判据 `check-skillpack636` 22/0，门禁 93/4 只剩 4 条老红；实测临时家目录下
  得到「已装 13 个知识包 · 来源：插件包」，发布包已切到 2026.09.14.387）。
  **⑦ 版本不匹配四选一已做**（四批：`a12d7b2` 待决台账 + 表态写回矩阵 38/0 · `5ed8d3d` 控制台自绘四选一弹窗 +
  `/api/decide` 32/0 · `88f9e2a` Persona Morph 自己弹且不抢前台 30/0 · `074d116` 五处联动 + 后台作业 39/0）。
  **⑧ P2 已做**（跑器 v4：`compat_run.py` 打「四套坐标口径对照」＋标定降噪判定 `runner_lib.calib_verdict`
  ＋光标归因拆成「布置/发送」两阶段，配 `README-口径与归因.md` 与离线判据 `selfcheck.py` 33/0；
  包 `_outbox\兼容性矩阵-跑器-r3-2026.09.14.zip` 9 文件 / 20.9 KB，关键文件与 v4 代码已核在包内）。
  余下：⑤ 跑器重发 —— **只差一步人工**：`_scratch\send_r3_e.py --dry` 已跑过，会话闸/身份闸都如实判否
  （当时开着的是「[文件]兼容性一」，不是 E）⇒ 把微信切到 E 的会话后跑 `py -3 _scratch\send_r3_e.py`
  即可发三个包（冷启动 r3 · 跑器 r3 · 群相在线包）；⑦d 真·托盘气泡**已做**（`agent/tray.py`，
  自研图标＋消息窗＋点气泡开控制台，判据 16/0，`--real` 22/0）。
  **⑤ 顺带修掉两个真缺陷**（2026-09-14 实测「切到 E 会失败」时挖出来的，判据都补了）：
  ① `chat_ocr.find_row_info` 的单字名字复核用**裸全等** `norm(got)==norm(name)`，而 E 那种行的名字行
  被 OCR 成 `[草稿]EE` ⇒ 单字母会话**永远定位不到**（改成与 `matches()` 同口径：剥草稿前缀、单字以它开头；
  假阳性仍被挡，因为挡它的是**名字行**本身）；② `wechat.switch_chat_posted` 只用"最后一条消息时间"
  这一条路认行，而草稿行**不显示时间戳** ⇒ 明明有行也认不出（补"先时间、后名字"两条路）。
  ⚠️ 自动切会话仍未成立：修完能定位到行、投递点击也发出去了，可**没看到该行变绿底、也 OCR 确认不到 E**
  （`没看到该行变绿底` 这句就是复核结论）⇒ 该环节按"未成立"记，不许靠"再点一枪"硬凑。
  **⑤ 已闭环（2026-09-14 晚）**：用户当面确认「微信就开在 E 的会话」，于是走 `confirm_open=True` 这条明路，
  三个包**已发出并全部 DB 回读确认**：`另一台机器冷启动包-2026.09.14-r4.zip`（local_id=700）·
  `兼容性矩阵-跑器-r3-2026.09.14.zip`（701）· `群相-在线包-20260914.zip`（702）。
  用户同时报告"明明一直在 E 的会话，你却扫不到"⇒ 查实**四个真缺陷**并当场修掉（判据 `session_pick_selftest` 68/68）：
  ① 身份闸的"针"混进**类型占位符**（`[文件/链接/卡片]`/`[文本]`）⇒ 永远匹配不上、还把真短 token 挤出名额；
  ② 聊天区**读不到字**时返回 False（"证据说不是"）⇒ 改成 None（"判据不可用"）；
  ③ `pane_text` 单帧原尺寸读，文件卡多的那屏读成空串 ⇒ 放大 + 分段重读（修完当场读到聊天区全文）；
  ④ `send_file_posted` 对内容档的 False **无条件拒绝** ⇒ `confirm_open`（用户当面确认）形同虚设，改成可压过并留痕。
- [✅] **长消息正文还原（2026-09-14 晚，用户报「我把你的话复制到微信发过去之后你就不太能正常识别了」）**：
  实测机制＝微信 4.x 把**长文本与文件卡**的 `content` 以 **zstd** 压缩存库（local_id=703 是 1791 字节
  zstd 帧），而库的友好化读法遇到压缩体**只回类型标签** `[文本]` ⇒ **正文整条消失**（短消息没压缩所以正常
  ——这正是"只有粘长文才出事"的原因）。修法：新增 `replica_adapter.recover_text/message_text/fill_text`
  （走库**公开** `get_message_row` + 运行时自带 `zstandard 0.25.0`，解不开就返回空串、绝不猜），
  接进 `recent_texts`（身份闸的针）与 `normalize`（模型输入，放在最前面让所有分支都吃到正文）。
  判据 `scripts/content_recover_selftest.py` **22/0**（含真数据：针里出现 **4 条长正文**、不再有纯标签）。
  **修完后的自证**：身份闸**自己**就认出了 E —— 内容档 True，命中的正是用户粘过来的那段长文本
  （`'我在另一台电脑上给你交接一个已经'…`）；冷启动包 r5 已发（**local_id=704**）。全量 54 个判据脚本全绿。
  **同日补发启动器安装件**：`Agent启动器-2026.09.14.387.zip`（49.4 MB）已发 E（**local_id=706**）—— 用户在那台
  机器上按我说的第 5 步找不到 `--selftest`（那是 **Agent启动器** 的开关，不是群相 `一键启动.exe` 的），
  且冷启动包从来只装"插件 + 补丁守护 + 知识包"、**不含启动器本体** ⇒ 已把两个启动器的区别写进冷启动文档第 7 条。
- [✅] **中文名「群相灵」→「群相」**（2026-09-14 用户口径：「把那个"灵"字删掉，中文就叫群相」）：31 个文件改名
  （代码/控制台/启动器与关闭器源码/文档/脚本/命令脚本），两个 exe 用 csc 重编并用 `--dlgprobe` 核过
  「群相 一键启动 / 群相 启动完成 / 欢迎使用 群相！/ 检测到 群相 控制台已在运行。」；
  **英文名 Persona Morph 一律没动**；历史归档（`docs\changelog-archive.md`、`报告\`）**保持当时写法不改**，
  并在 AGENTS.md / 更新日志.md 里写清两段改名记录。桌面已存在的旧快捷方式 `一键启动 群相灵.lnk` 未动
  （启动器只在缺失时才建，新名 `一键启动 群相.lnk`，旧名继续做兼容检查）。
- [⏳] **重启 DSH 验证插件**（下一步：用户重启 → 技能中心核对 25 插件 + 4 自研技能全部加载、无冲突 → 我复核）。
- [⏳] **Token 优化**：模拟真实对话测每轮 token 基线（当前可能 >2000/轮）→ 搜索主流方案（上下文截断/记忆压缩/工具提示精简/系统提示瘦身/多轮摘要缓存）+ 自研点 → 优化 → 循环实测**稳定 <2000 token/轮**。
- [⏳] **朋友圈内嵌形态真机验证**：弹窗形态已真机验证；内嵌（主窗右侧内容区）相机多点候选+OCR 逻辑已实现，待用户环境真发一条（dry 模式安全）确认。
- [⏳] **dsh 插件补装评估**：dsh-agent-teams（npm 无）、@JasonFreeLab/dsh-superpowers（GitHub 独占）——网络恢复后经 dsh-community-plugins 评估再装；装前确认无冲突。
- [⏳] **一键启动全链最后确认**（用户端口 1999 下，点两次：一次→单控制台；二次→提示窗不重开；日志"同版本/本次不重复打开"为证）。

## 已完成（近期，均已验证）

- [✅] **死键收口（2026-09-13）**：全量审计 229 个默认配置叶子键 → 真死键 6 个（`security.allow_private_image_hosts` 接线进 `image_sources` 的 SSRF 闸门；`memory.use_chat_model`/`provider`/`model`、`scoring.import_seed_file`、`ui.poke_degraded`/`poke_fail_count`/`moments_entry` 删除，`config.example.json` 与用户 `config.json` 同步清理）；新判据 `scripts/cfg_keys_selftest.py` **20/20**（含"塞假死键必须被抓到"的阴性对照）；21 个 selftest 全绿。
- [✅] DSH web "Failed to load plugins" 修复（根因：侧栏官方 client.js 被注入"// 注释+裸换行"→ 字符串语法错误 → 全部客户端插件加载失败；已从原版重建+正确重做 CSS 改动，playwright 实启验证 app 挂载+截图；reapply-patches v3.3 守护+vendor 原版+自愈；project-audit 新增侧栏健康检查）
- [✅] 双开控制台根治（端口感知探测 + 原子锁 + 单点打开 + Mutex 自愈；日志+截图验证通过）
- [✅] 微信窗口强制限位 1160×900（真机实测）
- [✅] 停止/重启生效（真机 shutdown→0 进程）
- [✅] 访问口令掩码 + 恢复端口按钮 + 端口保存提示（编译+联动检查）
- [✅] 安装依赖实时进度（@@PROG:install 逐包事件）
- [✅] 人设卡联网真实资料收集（注入补足与评分；接口实测 200/来源/语录）
- [✅] 群友深度印象（跨群采集+统计提炼；单元+接口实测）
- [✅] 机器学习仅默认卡启用（后端限制 + 说明）
- [✅] 图标全链路（exe 原图/窗口黑框/徽章透明白鲸；导出提取验证）
- [✅] 启动器全弹窗 UI 化（0 系统 MessageBox；csc 编译验证）
- [✅] 新机无 Python 无缓存启动测速 46.6s（写入更新日志）
- [✅] 启动器单点打开 + 弹窗联动确认；费用计算器（全厂商 171 条价目 + POST 修复 + 居中显示栏）

---
*规则更新方式：有新任务或新教训，立即更新本文件与 wx-dev / wx-verify skill（保持三处一致）。*
