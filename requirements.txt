# 群相（Persona Morph） 依赖清单（Python 3.10+，Windows 64 位）

# —— 微信接入（核心，无注入 UIA 模式）——
wechatauto-replica==1.2.2.2

# —— HTTP / 大模型调用 ——
requests>=2.28.2
urllib3>=1.26.0

# —— 图片处理（视觉识图 / 图片编码）——
Pillow>=9.0.0

# —— 微信数据库/媒体解密底层依赖（wechatauto 依赖它们）——
cryptography>=41.0.0
zstandard>=0.25.0
uiautomation>=2.0.18
comtypes>=1.4.0
pywin32>=305
pyperclip>=1.8.2
psutil>=5.9.0
colorama>=0.4.6
winsdk>=1.0.0b10
imageio-ffmpeg>=0.4.9

# —— 真语音条：把合成好的 PCM 播进虚拟声卡（VB-CABLE 之类），让微信自己录成语音条 ——
sounddevice>=0.4.6
pypinyin>=0.48.0

# ── 微信 4.x 驱动库 1.2.2.2 带来的新依赖（W1 实测：库内部 moment.py / uia_driver.py 直接 import cv2/numpy，
#     缺了会在「朋友圈/图像定位」相关功能上崩，所以必须进离线包）──
PyAutoGUI>=0.9.54
MouseInfo>=0.1.3
PyGetWindow>=0.0.9
PyMsgBox>=2.0.1
PyRect>=0.2.0
PyScreeze>=1.0.1
pytweening>=1.2.0
numpy>=2.2.6
opencv-python>=5.0.0.93

# ── 语音合成（默认音源，2026-09-15 跨机实测补）──────────────────────────────
# 为什么必须进这里：`voice_reply.backend` 的**默认档是 edge**（免费神经语音、不用 key、
# 音质接近真人），可它原来是「让用户自己 pip install」⇒ 干净机器上默认档直接不可用，
# 只能退回系统机械音（跨机报告里那行「选了 edge-tts 但本机没装它」就是这么来的）。
# 装不上时仍按 `voice_reply.edge_fallback`（默认开）退回系统声音并写明，不静默。
edge-tts>=6.1.0

# 说明：
# 1. 完全离线部署时，把上面所有包（含 wechatauto）的 .whl 放进 offline/wheels/，
#    然后 `pip install --no-index --find-links=offline/wheels -r requirements.txt`。
# 2. ⚠️ 2026-09-16 更正：`wechatauto-replica` **就在 PyPI 上**（实测 1.2.2.2 在 pypi.org/清华/阿里
#    都能下），本行旧注释说的"不是 PyPI 官方包、需从 offline/wheels 取"**已过时**——`offline/`
#    早已不在在线包里。干净机器联网装即可，不必再找 wheel。
# 3. 语音链路的 SILK 解码器 `pilk` 是**故意不列在这里**的（GPL-3.0、不随本项目分发）：
#    需要在产品里用「语音转文字」时按控制台给的命令 `pip install pilk` 自行安装，
#    没装时产品会如实说"没有可用的 SILK 解码器"，不会静默失败。
