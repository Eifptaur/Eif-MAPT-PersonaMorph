# 更新日志（Changelog）

按时间倒序，只记录**功能性**更新。

## 2026-09-14（控制台窗口/开窗/关闭/检测四件）

- **控制台窗口可拉伸放缩**：无边框窗口自己留一圈缩放环（环内像素归窗体）+ 自绘 `WM_NCHITTEST` 回八向缩放码；圆角裁剪随尺寸重算（原来只在构造时算一次，拉大后新区域点不动）。初始尺寸改为**按工作区比例 + 窗口 DPI 现算**（原来写死 1180×780 物理像素，125% 缩放的机器上只有 944×624 逻辑像素）。
- **左导航放大、拉开**：字号 13.5→14.5、图标 16→18、内边距 9→12、行距 1→4px、侧栏列宽 216→252。原来那几条声明写在**没有拼进 CSS 的裸字符串**里（Python 相邻字符串字面量只是"算一下扔掉"），浏览器从来没收到过 ⇒ 导航一直是一行行贴着的纯文字，现已落进真规则。
- **控制台只开一个窗**：所有开窗入口（启动器 / 机器人 / 托盘）统一到 `agent/notify_ui.open_console` 一处，共用一把锁（`logs/browser_opened.lock`），优先级＝自家 WebView2 窗口 → 浏览器；先查 WebView2 运行时是否就绪再决定。修掉"自家窗口和浏览器同时弹两个窗"。
- **修掉控制台 `{"error":"unauthorized"}`**：访问地址改由**拥有口令的一方**（控制台启动时）落盘到 `logs/console.url`，其他入口只读；不再有人手写字符串去 config.json 里找口令（旧写法抓到的是排在前面的 `cloud.token` 空串 ⇒ 拼出 `/?token=` ⇒ 401）。
- **一键关闭真的关得掉**：结束进程的入口脚本名从早已改名的 `wx_agent.py` 更正为 `persona_morph.py`（原来看门狗被杀、控制台子进程活下来，于是再点一键启动弹"为保持唯一…"），并按控制台端口兜底收掉占用者、关后**回读端口**如实报告"已释放 / 仍被占用"。
- **删除「程序鼠标检验」**（真鼠标一键检验）：面板、按钮、后端入口、路由与体检里的 11 项循环全部移除——它会接管用户的鼠标。"点击测试"保持 55 项，全程在程序内完成、不碰鼠标、不抢前台。
- **人设库新增分区「🎸 BanG Dream!」**（先加分区、再加卡）：第一批 Poppin Party 5 人（戸山香澄 / 市ヶ谷有咲 / 花園たえ / 牛込りみ / 山吹沙綾）。每张卡的设定与台词都来自**第一手来源**——官方角色页（bushimo 官方设定原文）＋黑白双翼 WS WIKI 卡面「台词」（游戏内语音原文，日文）；没有一手台词的卡在卡内标注「按其口吻」，出处逐条记在 docs/人设来源台账.md。
- **人设库 BanG Dream! 扩到 25 张**（第三、四批）：Roselia（湊友希那 / 氷川紗夜 / 今井リサ / 宇田川あこ / 白金燐子）、MyGO!!!!!（高松燈 / 千早愛音 / 要楽奈 / 長崎そよ / 椎名立希）、Afterglow（美竹蘭 / 青葉モカ / 上原ひまり / 羽沢つぐみ / 宇田川巴）、Pastel*Palettes（丸山彩 / 氷川日菜 / 白鷺千聖 / 大和麻弥 / 若宮イヴ）——设定全部取自官方角色页原文；只有官方页出现过引号的才当原话（あこ「自分もカッコ良くなりたい！」），其余在卡内标注「按其口吻」，台账同步记录。
- **人设库新建分区「🀄 东方Project」**：首批 博麗霊夢 / 霧雨魔理沙 / 十六夜咲夜。设定取自官方出版物原文（经 thwiki「一设资料」收录），魔理沙那条用了她的**官方原话**「不华丽就不是魔法了，弹幕最重要的是火力」；其余无引号原话者按口径标「按其口吻」。库内共 212 张卡 / 17 个分区。
- **人设库再扩两批**：🎸 BanG Dream! 加 Hello, Happy World!（こころ/薫/はぐみ/花音/ミッシェル）与 RAISE A SUILEN（レイヤ/ロック/マスキング/パレオ/チュチュ）共 10 张，该分区达 35 张；**新建分区 ⏳ 重返未来：1999** 首批 4 张（十四行诗/星锑/苏芙比/维尔汀）——全部用**游戏内本色语音与剧情原文**（维尔汀是主角、不在实装角色页里，台词按用户提示去剧情页抓）。库内共 226 张卡 / 18 个分区。
- **新人设卡全部补上模型评分 + 修掉一个系统性缺陷**：42 张新卡原来在控制台「模型分」一栏是空的；补分用的就是产品那把尺子（RULES_TEXT 五维细则）。补分同时发现并修掉真因——`persona_enrich` 的对话示例是**从卡里抓前三对引号自动拼**的，新卡里最先出现的引号往往是「强调词／官方档案字段」，于是示例变成「问今天好累 → 回『最普通』」这种胡话（评分 fit 维度当场掉到 22）。现在新卡自带 `## 说话规则（群聊通用）》+ 人工可控的 `## 对话示例`，enrich 见到这两段就跳过自动补。**补分前后：中位 57.0 → 68.4，低于 60 分的从 31 张降到 2 张**（区间 59.3~80.9）。
- **补足功能导向「贴合人设」**（用户口径）：补足 prompt 新增硬要求——当前卡里**已确认的真实台词/口癖/称呼必须原样保留**（那是角色指纹），只许补充不许替换成自造内容；并要求改写后自检「把名字盖住还认得出是谁吗」。
- **新增「反馈」栏**（左导航单开一栏）：用户在控制台里直接填（类型/内容/联系方式）→ 一键提交，程序自动把诉求整理成正文发出去（可走自建中转网址，或 SMTP 直发邮件）；没配通道时**如实显示「已存在本机、还没发出去」**并提供「补发」按钮，绝不留假成功。收件人/SMTP 授权码只存本机 config.json（不进代码与外发包）。
- **表单控件统一配色**：原来只有 `.row input[type=text]` 那一族被染色，**没写 `type` 属性的输入框**以及 `.row` 之外的输入框全落成浏览器默认白底（深色界面里最刺眼的正是它们：禁止词/观察词/音频目录/图库目录/工具目录/会话存档条数）。改为一条零特异性（`:where()`）兜底规则覆盖全部文本类输入框/文本域/下拉，且跟随主题换色；文件选择按钮也一并配色。
- **「鲸语」文案补齐到全界面**：原来字典只有 50 条（导航 + 面板标题 + 十来个按钮），切过去"只有少数几个变了"。现在 441 条覆盖 **432 条可见短文案**（面板标题 / 行标签 / 按钮 / 表头 / 导航），并改成**整节点替换**（只换"整个文本节点"）——修掉"「停止」吃掉「停止检测」"这类子串误替换；导航另用一张短表（窄列不会被省略号截断）；字典单一来源在 `agent/whale_text.py`，服务端与前端共用一份，前端不再手写第二份。
- **启动器新增两条取证探针**：`一键启动.exe --winprobe`（窗口几何/缩放命中码，不显示任何窗口）· `--urlprobe`（口令来源，只打印掩码与长度）；浏览器回退时把原因写进 `logs\console_open.log`。

## 2026-09-10（一键启动进度窗口）

> **改名记录**：2026-09-13 产品定名 **群相灵（Persona Morph）**；**2026-09-14 中文名再改为「群相」（英文名 Persona Morph 不变）**。历史归档（`docs\changelog-archive.md`、`报告\`）保持当时的写法不改。

- **一键启动窗口**：启动过程实时可见——绿色版 Python 下载显示百分比、依赖安装进度条、自检逐项滚动；全部成功后窗口自动关闭，失败弹窗说明原因。
- **修复**：依赖/自检输出曾因管道缓冲在窗口长时间无下文（实际在后台安装），现改为行级实时刷新，静默超过 30 秒会提示"仍在运行"。
- **停止机器人**：修复 Python 路径读取换行与脚本括号语法错误（都导致停止调用无法运行）；机器人未运行时提示"已处于停止状态"（不再误报停止失败）。
- **Python 适配**：系统 Python 超出 3.10~3.12（如新版 3.13/3.14）时自动改用内置绿色版 3.10——依赖中的 winsdk 预编译版最高只支持 3.12，新版 Python 会现场编译失败导致装不上（本次正是踩到此坑）。
- **首次配置**：API Key 未填写（占位符）时自检降级为提示项，一键启动照常完成并自动打开控制台——到「模型 API」页填密钥即可，不再因"未配置密钥"而中止启动。
- **控制台**：浏览器自动打开不再被历史标签页残留拦截（每次启动机器人都会弹一次控制台）；控制台为标准 HTTP 网页，兼容 Chrome/Edge/Firefox/360/QQ 等现代浏览器。
- **看门狗**：修复拉机器人的解释器选错系统版 pythonw 的问题（如系统 Python 3.14 在 PATH 时会选中，而依赖装在绿色版 3.10 里 → 机器人启动即崩、控制台起不来）——现在一律用与一键启动同版本的解释器；旧版看门狗残留会由新版自动结束并接管。
- **主程序兼容性**：修复内置绿色版 Python（embed）下 persona_morph 启动找不到模块的问题（此前后台拉起即报 `No module named 'agent'` 反复崩溃，控制台永远起不来）——启动时自动把程序目录加入模块搜索路径。
- **一键启动收尾**：拉起机器人后轮询等待控制台就绪（微信布局校验等可能耗时数十秒）再打开浏览器，并显示等待进度；不再因"只试一次没弹"而漏掉浏览器，窗口在全部完成后自动关闭。
- **浏览器打开**：自动探测 Chrome/Edge 等并优先使用（Chrome 优先；Server 系统/未设置默认浏览器也能弹），支持 `config.json` 的 `server.browser_path` 指定任意浏览器（如 QQ 浏览器）路径；启动的两段窗口分别带标题（准备环境 / 一键启动）。
- **控制台优先启动**：Web 控制台先于微信窗口几何调整启动（微信 UIA 布局校准可能耗时数十秒，此前会拖住控制台导致浏览器弹不出/打不开）。
- **联网搜索**：DeepSeek 联网搜索优先（自动复用主 API Key，返回综合答案），不可用时免费引擎自动回退（Google → Bing，无需 key）。
- **微信操作**：朋友圈入口自适应——新 UI（微信 4.1 发现页：点「发现」→ OCR 定位「朋友圈」）与旧 UI（侧栏相机图标）自动识别，朋友圈独立窗口/微信内嵌两种形态均可验证；窗口清理与点击整备不再触碰浏览器窗口，重叠窗口改为**置于下层**（不再最小化收起）；微信窗口按 DPI 换算并限制在屏幕内；控制台自定义光标全局覆盖（含挂件区域）。
- **体检**：首次引导改为代码与依赖级检测（不动鼠标、秒级完成）；点击测试任一项失败即终止，不再机械跑完。
- **控制台弹窗**：浏览器原生弹窗（alert/confirm/prompt）全部替换为控制台内弹窗（提示条/确认框/输入框），不再出现浏览器自带的"127.0.0.1 显示"弹窗。
- **表情发送**：收藏表情改为本地收藏夹直发（不受微信面板布局与用户预收藏顺序影响）；微信面板点击发送保留为兜底。
- **数据迁移**：概览栏新增「导出记录 / 迁移数据」——计费与对话记录可导成一个迁移包（zip，按日期文件），另一台电脑选文件导入（自动合并、同内容去重）；一键启动完成后弹窗询问创建桌面快捷方式（带鲸鱼图标）；控制台鲸鱼图标重绘（深蓝描边，轮廓更清晰）。
- **全新环境启动速度（2026-09-09 实测）**：无 Python、无缓存的纯新机 ~47 秒到控制台（准备绿色版 Python 下载+解压 ~31s → 依赖安装 → 自检 → 启动机器人+浏览器 ~46.6s 总）——绿色版 Python 联网下载约占大头，之后机器有 runtime/缓存会明显更快。
- **一键启动启动器**：不再弹 PowerShell/cmd 黑窗——改为图形启动器窗口（鲸鱼图标 + 四步清单 + 实时进度条 + 状态文字），后台隐藏执行准备 Python/依赖/自检/启动，完成后询问创建桌面快捷方式，窗口自动收尾。
- **精简入口**：移除根目录「安装依赖.bat」（一键启动已自动完成依赖安装，保留会重复入口）。

## 2026-09-09（一键启动健壮性）

- **一键启动（无 Python 新电脑）**：联网下载绿色版 Python 的目标目录修正（源码包没有 offline 目录时不再下载失败）；依赖安装失败自动切换镜像源重试（清华 → 阿里 → 官方）。
- **失败提示**：一键启动失败弹窗显示具体失败项（依赖安装失败原因 / 自检失败项明细），不再只有笼统提示。

## 2026-09-08（vβ·Ⅱ 最终版）

- **计费日志管理**：概览右上角「一键删 / 勾选删」——勾选删弹窗按真实账本按天聚合（年月日/次数/会话数），可定位任意日期（年 6 档/月/日联动），确认后真实删除当天记录并回补累计，概览自动刷新；一键删=清空全部计费并归零。
- **代码检测**：检测中心「代码检测 / 代码检测＋依赖核对」实时逐项进度、完成/失败均有状态条与 toast；检测结果保留在概览常驻状态条。
- **人设评分与补足**：统一权威评分细则（风格/贴合/一致/自然/完整，0.01 精度；网梗、通用模板、AI 腔等缺陷强制压分；禁止"指出缺陷还给高分"）；模型补足每轮同尺子评分，最终 3 次复评取中位，自动清除补足输出的多余文字。
- **人设库**：185 张角色卡全量重写（两步式：先确认角色原话再重写）；全卡二轮原话核查与人工修正，评分全唯一（77.41~88.36）。
- **记忆**：关机总结印象修复（此前从未执行+写回调方法不存在）——停止时自动总结最近对话为成员印象并落盘；记忆页成员印象显示修复；删除已删群的"幽灵"缓存。
- **一键启动（零环境）**：自动检测/安装 Python（系统 Python → 离线绿色版解压 → 联网下载），自动装依赖、55 项自检、拉起机器人；停止/开机自启同套流程；安装失败有中文弹窗提示。
- **多厂商兼容**：OpenAI 兼容协议客户端；非 DeepSeek 地址不发送私有参数、DeepSeek 地址支持思考模式开/关；上游错误可重试；中转站地址任意填。
- **发布包**：两包拆分说明（源码包=联网安装；离线包=含绿色 Python+依赖+微信安装包）；`海的眼睛.mp4` 背景视频不再打包（源码包 3.3MB / 离线包 319.3MB）。
- **修复**：暂停按钮 404（路由缺失）、配置修改被旧值覆盖、勾选删除空日志、停止必停、文件依赖版本核对异常等。

## 2026-09（早期，功能从无到有）

- 微信监听/发送/引用/拍一拍；角色库+自定义人设；记忆与印象；反应评分；联网搜索；图片识别；会话/成本统计与日历；码检测/鼠标检测；看门狗与自启；控制台全部设置项；种子库导入导出；备份配置。
- 具体条目见历史存档 `docs\changelog-archive.md`（本文件仅保留纯功能摘要）。

## 致谢

本作品整合自以下开源项目（经二次开发整合，仅供学习交流；上游版权归原作者，许可证随包附送）：
- QQ Agent（智能大脑）：由 B 站网友 Kondius 基于 qq-bridge 修改而来
- wechat-deepseek-bot（微信接入层，底层 wechatauto UIA 无注入）
- DeepSeek-Balance-Whale-Widget（小鲸鱼余额挂件）

兼容与新增功能工作量相当于重构。
