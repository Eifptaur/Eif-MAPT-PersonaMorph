没装微信就带他去装：三态检测 + 控制台提示卡 + 一键重测（不做静默安装）

用户 2026-09-13 要求：「Persona morph 加上一个功能：检测到用户没有微信时帮他安装，离线包就不需要了」。

- agent/wechat.py 新增 wechat_install_state()：只看三个只读来源（注册表卸载项 / 常见安装路径含非默认盘 /
  开始菜单快捷方式），给出三态 missing / installed_not_running / running，附 official_url 与建议动作；
  并入 wechat_version_info() 返回（老字段一动不动，只加 state/installed/install）。
- 「帮他装」的边界：不做静默安装（要下安装包 + UAC，也不该替用户动系统）——只带他去官网 + 一键重测。
- 控制台「微信」区块：新增提示卡（默认隐藏，只在没装/没跑时出现），两个动作「打开官网下载」「我装好了，重新检测」；
  文案明写不会替你静默安装。webui：/api/status 带 wechat_install，新增 /api/wechat/recheck。
- 判据：scripts/wechat_install_selftest.py 24/0（三态映射 / 函数体不许有 subprocess·os.system·ShellExecute·
  msiexec·写注册表 / 三条只读来源在位 / 文案 / 接线（用真正会被服务出去的 console_html.HTML 断言）/ 真机三态与老字段不回归）；
  回归 selftest.py 55/55。README 与使用说明同步。
